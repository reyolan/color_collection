<div class="h-[calc(100%-88px)] bg-slate-600 p-4 overflow-auto">
  <h1 class="text-5xl font-bold text-white text-center">All Colors</h1>
  <%= render 'shared/success_flash'%>
  <div class="flex gap-6 flex-wrap bg-slate-700 rounded-md p-6 w-fit m-auto mt-6 justify-around text-slate-200">
    <% if @colors.count > 0 %>
      <% @colors.each do |color| %>
        <div class="flex flex-col justify-center items-center">
          <div class="h-32 w-32 rounded-md" style="background-color: #<%= color.hex %>" ></div>
          <%= link_to color[:name], color, class: "text-lg hover:underline"%>
        </div>
      <% end %>
    <% else %>
      <p class="text-2xl font-semibold text-center">There are no colors stored currently.</p>
    <% end %>
  </div>
</div>
